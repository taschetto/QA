      <!-- Sidebar -->
   
      <aside class="large-3 columns">
        <h5>Unidades Acadêmicas</h5>
        <ul class="side-nav">
          <%
            @pucrs = Category.where("name = '#{QA::Application.config.main_category}'").first
            if @pucrs != nil
              Category.where("ancestry = '#{@pucrs.id}'").find_each do |category| %>
                <li><%= link_to category.name, category %></li>
          <% end
          end %>
        </ul>
   
        <div class="panel">
          <h5>Você sabia...</h5>
          <p>Que nós somos muito legais?</p>
          <a href="#">Leia mais &rarr;</a>
        </div>
   
      </aside>
   
      <!-- End Sidebar -->